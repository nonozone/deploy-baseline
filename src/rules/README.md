# Product Rules Source

This directory is the canonical source for `deploy-baseline-kit` product policy, convergence rules, and exception handling guidance.

During Phase 1, existing skill references still live under `skills/deploy-baseline-kit/references/`, but new rule work should converge here and later be packaged into the installable skill.
