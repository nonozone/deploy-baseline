# Product Docs Source

This directory is the source area for product-facing documentation that should ship with the installable `deploy-baseline-kit` package.

During Phase 1, the repository still keeps historical docs under `docs/`, but new product-facing packaging work should converge here instead of creating more parallel copies.

Current source docs:

- `product-overview.md`
- `install.md`
- `usage.md`
- `scope.md`
