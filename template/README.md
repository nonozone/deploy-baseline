# 通用模板骨架说明

这个模板目录用于复制到新项目中作为初始骨架。

复制后需要至少完成以下动作：

1. 替换项目名、服务名、镜像名
2. 替换 `scripts/` 与 `deploy/scripts/` 中的占位逻辑
3. 根据项目实际情况补齐 `deploy/README.md`
4. 填写 `.env.example` 与 `deploy/env/*.example`

模板默认提供：

- 标准 `Makefile`
- 三层 Compose 结构
- 本地脚本目录
- 部署脚本目录
- 中文部署文档模板

模板不会替你决定项目技术栈，只负责提供统一工程接口。
